version: "3.9"
services:
  url-shortener:
    build: .
    ports:
      - "9003:9003"
    env_file:
      - .env
    volumes:
      - ./short_urls.db:/app/short_urls.db
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
